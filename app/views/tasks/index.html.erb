<h1>Tasks</h1>


<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= task %>
      <%= link_to task.title, task_path(task) %>
      <!-- <%= task.details %> -->
      <%= task.completed %>
    </li>
  <% end %>
</ul>

<%= link_to 'create new task', task_new_path %>
